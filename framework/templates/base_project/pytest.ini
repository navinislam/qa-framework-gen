[pytest]
markers =
    smoke: Quick smoke tests for critical functionality
    regression: Full regression test suite
    ui: UI-level tests
    selenium: Selenium-based tests

addopts =
    --alluredir=allure-results
    --clean-alluredir

base_url = {{BASE_URL}}
